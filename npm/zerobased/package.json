{
  "name": "zerobased",
  "version": "0.0.1",
  "description": "Zero-config Docker service router — auto-detects containers, creates Unix sockets + HTTP routes",
  "bin": {
    "zerobased": "bin/zerobased"
  },
  "scripts": {
    "postinstall": "node install.js"
  },
  "optionalDependencies": {
    "@lagz0ne/zerobased-linux-x64": "0.0.1",
    "@lagz0ne/zerobased-linux-arm64": "0.0.1",
    "@lagz0ne/zerobased-darwin-arm64": "0.0.1",
    "@lagz0ne/zerobased-darwin-x64": "0.0.1"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/lagz0ne/zerobased"
  },
  "keywords": ["docker", "devtools", "unix-socket", "zero-config", "caddy", "reverse-proxy"]
}
